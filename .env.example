# Copy to .env and fill in.
# Get a key from https://aistudio.google.com/apikey
# Deliberately not named GEMINI_API_KEY so the SDK doesn't auto-pick it up
# from env; the app reads it explicitly. Isolates spend to this project.
JOB_MATCHER=your-key-here
