# Core (needed for live fetching)
requests>=2.31

# Recommended: semantic matching. Without it the tool falls back to a lexical
# (bag-of-words) scorer that still runs but matches far worse.
sentence-transformers>=2.6

# Optional: only needed to read PDF resumes (.txt/.md work without it)
pdfplumber>=0.11

# Gemini embedding backend and resume-structuring hook (reads JOB_MATCHER env var)
google-genai>=1.0

# HTTP API (only needed if you run `uvicorn ats_matcher.api.main:app`; CLI works without them)
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
